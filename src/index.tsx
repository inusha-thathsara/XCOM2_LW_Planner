import React from 'react';
import './index.css';
import { render } from 'react-dom';
import { App } from './App';
// Render the app with Dashboard as the default route
render(<App />, document.getElementById('root'));